import LegalDocumentPage from "./LegalDocumentPage";

export default function PrivacyPolicyPage({ onBackToSettings }) {
  return <LegalDocumentPage documentId="privacy" onBackToSettings={onBackToSettings} />;
}
